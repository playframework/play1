# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /java                   JavaConsole.index
POST    /java			JavaConsole.index	
GET     /scala                  ScalaConsole.index
POST    /scala			ScalaConsole.index	
GET     /db                   	DbConsole.index
POST    /db			DbConsole.index	
GET    /			InfoConsole.index
# Map static resources from the /app/public folder to the /public path
GET     /public/                                staticDir:public

# Catch all
*       /{controller}/{action}                  {controller}.{action}
