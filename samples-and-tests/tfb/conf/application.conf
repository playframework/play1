application.name=tfb
application.mode=prod
application.secret=8f3a2b1c4d5e6f7a8b9c0d1e2f3a4b5c6d7e8f9a0b1c2d3e4f5a6b7c8d9e0f1a
http.port=9000

# Thread pool: default is (CPUs+1). pool=64 is optimal for H2 read benchmarks
# on this machine; increase further for PostgreSQL (match db.pool.maxSize).
# Note: updates degrade with more threads on H2 due to MVStore write contention —
# this is H2-specific and does not apply to PostgreSQL.
play.pool=64

db=mem
db.pool.maxSize=64
db.pool.minSize=64
jpa.ddl=create

# To benchmark against PostgreSQL, replace db=mem with:
# db.url=jdbc:postgresql://localhost:5432/hello_world
# db.driver=org.postgresql.Driver
# db.user=benchmarkdbuser
# db.pass=benchmarkdbpass
# db.pool.maxSize=256
# play.pool=256
# jpa.dialect=org.hibernate.dialect.PostgreSQLDialect
# jpa.ddl=none
